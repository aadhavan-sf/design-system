# Organisms

Organisms are complex reusable sections built from molecules and atoms.

Examples that belong here:

- `Modal`
- `TopNavigation`
- `Sidebar`
- `LeftPanel`
- `Table`
- `SettingsPanel`
- `LayoutsPanel`

Create an organism when a pattern represents a meaningful section of an interface, not just a single form control.
